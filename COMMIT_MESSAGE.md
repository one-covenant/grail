chore(lint): fix import sorting and formatting in trust list files

Apply ruff auto-fixes: sort imports in service.py and test_trust.py,
remove unused imports (json, SimpleNamespace, TRUST_LIST_MAX_STALENESS_WINDOWS),
and reformat constants.py line wrapping.

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
